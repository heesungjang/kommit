version: 2

project_name: kommit

before:
  hooks:
    - go mod tidy

builds:
  - main: .
    binary: kommit
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X github.com/heesungjang/kommit/internal/version.Version={{.Version}}
      - -X github.com/heesungjang/kommit/internal/version.Commit={{.Commit}}
      - -X github.com/heesungjang/kommit/internal/version.Date={{.Date}}

archives:
  - format: tar.gz
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: 'checksums.txt'

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'

brews:
  - repository:
      owner: heesungjang
      name: homebrew-tap
    homepage: "https://github.com/heesungjang/kommit"
    description: "Terminal-native git client with built-in AI and PR integration"
    license: "MIT"
    install: |
      bin.install "kommit"
    test: |
      system "#{bin}/kommit", "--version"

release:
  github:
    owner: heesungjang
    name: kommit
  draft: false
  prerelease: auto
  name_template: "v{{.Version}}"
